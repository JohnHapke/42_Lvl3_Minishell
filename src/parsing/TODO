1. implementation of parsing specific error_handling especially in token validator which is a bit to complicated built at the moment

2. revise variable asignation to be simpler and closer to bash
	Variable assignation can only be at start of line or first word after pipe
	Variables can be temporary for certain commands